{
  "buildCommand": "curl -LsSf https://astral.sh/uv/install.sh | sh && export PATH=\"$HOME/.local/bin:$PATH\" && uv sync --group docs && uv run sphinx-build -b html docs docs/_build",
  "outputDirectory": "docs/_build",
  "framework": null
}
